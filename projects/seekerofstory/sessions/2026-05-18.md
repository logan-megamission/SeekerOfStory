# Session Log — 2026-05-18

## Project
seekerofstory

## What happened
- User reported Vercel env vars kept disappearing; diagnosed root cause as likely environment-scope mismatch in Vercel dashboard (Production vs Preview vs Development tabs)
- User confirmed vars were set in Production scope and wanted to push from local `.env.local` rather than pull from Vercel
- Pushed 3 env vars from `.env.local` to Vercel production using `vercel env add`:
  - `BLOB_READ_WRITE_TOKEN`
  - `CLERK_SECRET_KEY`
  - `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`
- `VERCEL_OIDC_TOKEN` was intentionally skipped (Vercel auto-provisions this)
- `DATABASE_URL` was already present in production (set 29m prior to session)
- Warning: `BLOB_READ_WRITE_TOKEN` may have been stored with literal surrounding quotes — user should verify the value in `.env.local` is not wrapped in quotes

## LOG-136 progress
- [x] Neon DATABASE_URL added to Vercel production
- [x] Clerk keys added to Vercel production
- [x] Blob token added to Vercel production
- [ ] Pull locally: `vercel env pull .env.local --yes`
- [ ] Push schema: `npx drizzle-kit push`
- [ ] Seed founders: `npx tsx src/lib/seed.ts`
- [ ] Verify: `npm run dev` → localhost:3000
- [ ] Add YouTube/Spotify/Apple URLs for founders via /admin/founders

## Next session actions
1. Fix `BLOB_READ_WRITE_TOKEN` if it has literal quotes stored (re-add without quotes)
2. Run `npx drizzle-kit push` to create DB tables
3. Run `npx tsx src/lib/seed.ts` to seed Carrie + Yoel
4. Smoke test at localhost:3000 then proceed to LOG-137 (admin panel)
